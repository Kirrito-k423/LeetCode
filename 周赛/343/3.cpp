#include <bits/stdc++.h>

using namespace std;


class Solution {
public:
    int minimumCost(vector<int>& start, vector<int>& target, vector<vector<int>>& specialRoads) {
        //难点：不仅是背包问题，选还是不选，而且顺序会影响结果
		//动态规划，有一个是当前选的路径，
    }
};